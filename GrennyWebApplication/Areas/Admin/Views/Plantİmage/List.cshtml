@using GrennyWebApplication.Areas.Admin.ViewModels.Plantİmage
@model PlantİmageViewModel

<div>
    <div class="row">
        <div class="col-sm-12">
            <div class="page-title-box">
                <div class="float-end">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                            <a asp-route="admin-plant-list">Plants</a>
                        </li><!--end nav-item-->
                        <li class="breadcrumb-item active">ProductImage</li>
                    </ol>
                </div>
                <h4 class="page-title">Plants</h4>
            </div><!--end page-title-box-->
        </div><!--end col-->
    </div>
</div>

<div class="col-lg-12">
    <div class="row justify-content-end">
        <div class="">
            <a asp-route="admin-plantimg-add" asp-route-plantId="@Model.PlantId" target="_blank" class="btn btn-secondary">Add new plant image</a>
        </div>
    </div>
</div>

<div class="col-lg-12">
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Image</th>
                            <th>Created at</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var plantImage in Model.Images!)
                        {
                            <tr>
                                <td><img src="~/@plantImage.ImageUrl" width="20%" /></td>
                                <td>@plantImage.CreatedAt.ToString("dd/MM/yyyy")</td>
                                <td class="text-end">
                                    <a asp-route="admin-plantimg-update" asp-route-id="@plantImage.Id"><i class="las la-pen text-secondary font-16"></i></a>
                                    <form method="POST" asp-route="admin-plantimg-delete" asp-route-productId="@Model.PlantId" asp-route-plantImageId="@plantImage.Id" id="book-image-form-@plantImage.Id"></form>
                                    <button type="submit" form="book-image-form-@plantImage.Id">
                                        <i class="las la-trash-alt text-secondary font-16"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div>
